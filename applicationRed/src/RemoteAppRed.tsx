import React, { useState } from 'react'
import log from './common'

function RemoteAppRed(): JSX.Element {
  return <div style={{border: '2px red solid'}}>Just a red remote module!</div>;
};

export default RemoteAppRed;
